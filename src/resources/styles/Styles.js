import { StyleSheet } from 'react-native';
            const TabsColor = '#0e0e43';
            const iconColor = '#b31212';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 