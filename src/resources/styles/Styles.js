import { StyleSheet } from 'react-native';
            const TabsColor = '#4f4f8e';
            const iconColor = '#d86e6e';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 