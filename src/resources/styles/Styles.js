import { StyleSheet } from 'react-native';
            const TabsColor = '#0f0f79';
            const iconColor = '#a00000';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 