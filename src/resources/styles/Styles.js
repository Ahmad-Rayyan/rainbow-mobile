import { StyleSheet } from 'react-native';
            const TabsColor = '#f88a7c';
            const iconColor = '#1e2f33';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 