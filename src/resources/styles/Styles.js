import { StyleSheet } from 'react-native';
            const TabsColor = '#7c84f8';
            const iconColor = '#fff';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 