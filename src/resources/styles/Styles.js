import { StyleSheet } from 'react-native';
            const TabsColor = '#1a1a83';
            const iconColor = '#a21c1c';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 