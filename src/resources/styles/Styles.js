import { StyleSheet } from 'react-native';
            const TabsColor = '#445852';
            const iconColor = '#ffffff';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 