import { StyleSheet } from 'react-native';
            const TabsColor = '#4a9004';
            const iconColor = '#e8e5e5';
            const TabsStyle = StyleSheet.create({
                backgroundColor: {
                    backgroundColor: TabsColor
                },
                iconColor: {
                    color: iconColor
                }
            });
            export { TabsStyle }; 