import { StyleSheet } from "react-native";

const TabsColor = "#5e5eff";
const iconColor = "#ffffff";

const TabsStyle = StyleSheet.create({
  backgroundColor: {
    backgroundColor: TabsColor
  },
  iconColor: {
    color: iconColor
  }
});

export { TabsStyle };
