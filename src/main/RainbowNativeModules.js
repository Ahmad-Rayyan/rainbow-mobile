import { NativeModules } from 'react-native';

module.exports = {
    Authentication: NativeModules.Authentication,
    WebrtcModule: NativeModules.Webrtc,
    ConversationsModule: NativeModules.Conversations
};